<h1>Track Result</h1>

<div id="trackAllContainer">
{{#each allTracks}}
  <p>Title: {{this.name}}</p>
  <figure>
    <figcaption>Listen to the T-Rex:</figcaption>
    <audio
        controls
        src="{{this.preview_url}}">
            Your browser does not support the
            <code>audio</code> element.
    </audio>
  </figure>
{{/each}}
</div>


