<HTML>

<div class="album-poster">

    <h3>{{this.name}}</h3>

    <img src="{{this.images.0.url}}" alt="artist image for {{this.name}}">

    <button>
        <a href="/albumView?id={{this.id}}">Album: {{this.name}}
    </button> </a>

</div>

</HTML>