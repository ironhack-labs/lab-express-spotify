<div class="artist">
  {{#each artistsFound}}
  <h2> {{this.name}}</h2>
  <img src={{this.images.1.url}} alt={{this.name}} />
  <a href="/albums/{{this.id}}">View albums: {{this.name}}</a>
  {{/each}}
</div>