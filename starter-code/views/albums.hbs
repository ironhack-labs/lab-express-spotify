<main>
    <h2>Albums</h2>
    
        {{#each albumsList}}
        <div>
            <h3>{{this.name}}</h3>
            <img src={{this.images.0.url}}>
            <a href="/album/tracks/{{this.id}}">Show tracks</a>
        </div>
        {{/each}}
    
</main>