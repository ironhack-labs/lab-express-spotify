<div class="main-container">
    {{#each albums}}
    <div id="artist-container">
        <h1>{{name}}</h1>
        <img src="{{images.0.url}}" alt="{{this.name}}">
        <button><a href="/tracks/{{id}}">Go to Songs</a></button>
    </div>
    {{/each}}
</div>