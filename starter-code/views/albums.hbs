<div class="album">
  {{#each albums}}
  <h2> {{this.name}}</h2>
  <img src={{this.images.1.url}} alt={{this.name}} />
    <a href="/tracks/{{this.id}}">View tracks: {{this.name}}</a>
  {{/each}}
</div>