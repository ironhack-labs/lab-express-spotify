<div>
    <main>
        <h1>Artists albums</h1>
        {{#each albumsList}}

        <img src="{{{this.images.0.url}}}" alt="tapa-album-img">
        <h2>{{this.name}}</h2>
        <a href="/tracks/{{this.id}}">View Tracks</a>


        {{/each}}
    </main>
</div>