<div>
    <main>
        <h1>Tracks</h1>
        {{#each songsAlbum}}

        <h2>{{this.name}}</h2>
    <audio
        controls
        src="{{{this.preview_url}}}">
            Your browser does not support the
            <code>audio</code> element.
    </audio>
        {{/each}}
    </main>
</div>
