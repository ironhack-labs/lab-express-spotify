const { spotifyApi } = require("../spotify.config.js")

exports.searchArtist =  (req,res) => {
    // const results = spotifyApi.searchArtist(req.body.name)
    // console.log(results)
    // res.send(results)
}


