<div>
    <div>
    {{#each artistsArray}}
        <p>{{this.name}}</p>
        <img src="{{this.images.0.url}}" alt="Artist image">
        <a href="/albums/{{this.id}}"><button>View Albums</button></a>
    {{/each}}
    </div>

</div>