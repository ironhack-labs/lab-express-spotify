<div class="background">
  <div>
    {{#each artistsResults.items}}
    <div class="card">
      <img src="{{this.images.0.url}}" alt="artist image">
      <p>{{this.name}}</p>
      <a href="/albums/{{id}}">View Albums</a>
    </div>
    {{/each}}
  </div>

</div>