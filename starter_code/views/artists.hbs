<div class="artists">
  {{#each artist}}
<div class="artist">
  <h2>{{this.name}}</h2>
  <img src="{{this.images.0.url}}" alt="">
  <a href="{{../host}}/albums/{{this.id}}">View Albums</a>
</div>
{{/each}}
</div>

