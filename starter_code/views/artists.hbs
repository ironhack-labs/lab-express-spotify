<div class="listArtists">

  {{#each paco }}
  {{!-- {{> artists}} --}}
  <h2>{{this.name}}</h2>
  <img src="{{this.images.0.url}}" alt="artist">
  <a href="/albums/{{this.id}}">View Albums</a>
  {{/each}}



</div>