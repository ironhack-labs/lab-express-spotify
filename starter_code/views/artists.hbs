<div class="container">
  {{#each anghy}}
  <div class="artists">
    <img src="{{this.images.0.url}}" width="200" height="250">
    <div class="info">
      <h4>{{this.name}}</h4>
      <a href="/albums/{{this.id}}">View Albums</a>
    </div>
  </div>

  {{/each}}
</div>