<div class="artists-container max-container container">   
    {{#each dataArr}}
        <div class="card">
            <img src={{images.0.url}} alt={{name}}>
            <h2 class="artist">{{name}}</h2>
            <a href="/albums/{{id}}">View Albums</a>
        </div>
    {{/each}}
</div>