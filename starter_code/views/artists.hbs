<div class="container">
<div class="row">
    {{#each artists}} 
        <div class="col-4">
        <img width="300px" src="{{this.images.[0].url}}">
        <p>{{this.name}}</p>
        <a href="/albums/{{this.id}}">View Albums</a>
        </div>
    {{/each}}
</div>
</div>