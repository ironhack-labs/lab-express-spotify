<div class="container">
    <div class="d-flex flex-column">
    {{#each list}}
        <div class="artist p-2">
            {{this.name}}
            <audio controls src={{this.preview_url}}>
                Your browser does not support the audio element.
            </audio>    
        </div>
    {{/each}}
    </div>
</div>