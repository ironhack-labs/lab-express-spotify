{{#each theTracks}}
<h1>{{name}}</h1>
<figure>
    <figcaption>Listen to {{name}}:</figcaption>
    <audio
        controls
        src={{preview_url}}>
            Your browser does not support the
            <code>audio</code> element.
    </audio>
</figure>
{{/each}}