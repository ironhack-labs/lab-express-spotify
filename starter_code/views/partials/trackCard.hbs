


<article class="track-card">
<figure>
    <figcaption class="song-title" >{{this.name}}</figcaption>
    <audio
        controls
        src={{this.preview_url}}>
            Your browser does not support the
            <code>audio</code> element.
    </audio>
</figure>

</article>