<h1>view tracks ..</h1>

<div class="container">
  <div class="row">
    {{!-- <div class="card-deck"> --}}
        {{#each trackArray }}
      
          {{!-- {{> _onealbum this }} --}}
         
         <figure>
    <figcaption>Listen to the T-Rex: {{ this.name }}</figcaption>
    <audio
        controls
        src="{{ this.preview_url }}">
            Your browser does not support the
            <code>audio</code> element.
    </audio>
</figure>
<p>{{ this.preview_url }}</p>
        {{/each}}

    {{!-- </div> --}}
  </div>
</div>