<div class="cards">
  {{#each items}}
  <div class="card">
    <div>
      <img src={{images.0.url}} alt="" height="250px" width="250px">
    </div>
    <h3 class="name">{{name}}</h3>
    <a href='/tracks/{{id}}'><button class="button" type="submit">View Albums</button></a>
  </div>
  {{/each}}
</div>