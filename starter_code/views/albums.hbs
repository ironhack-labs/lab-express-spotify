<div class="musicContainer">
    {{#each albumsArtist}}
    <div class="musicAll">
        <img class="imgMusic" src="{{images.0.url}}" alt="Error Photo">
        <h1>{{this.name}}</h1>
        <a href="/tracks/{{id}}">View Tracks</a>
    </div>
    {{/each}}
</div>