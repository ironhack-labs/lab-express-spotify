<div class="artist-list">
    {{#each albums}}
    <div class="artist">
        <img src="{{this.images.1.url}}" alt="images">
        <div>
            <h6>{{this.name}}</h6>
            <a href="/tracks/{{this.id}}">View Tracks</a>
        </div>
       
    </div>
    {{/each}}
</div>