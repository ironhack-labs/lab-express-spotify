{{#each items as |album| }}
  <div>
      <img src="{{ album.images.[1].url }}" alt="test">
      <h3> {{ album.name }}</h3>
       <a href="/tracks/{{album.id}}">View Tracks</a>
    </div>
  </div>
{{/each}}