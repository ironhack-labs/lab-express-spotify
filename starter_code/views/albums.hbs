<h1>Albums</h1>
<ul>
    {{#each returnedAlbums}}
    <li>
        <img src="{{images.0.url}}" alt="{{name}} Image">
        <p>{{name}}</p>
        <a href="/album/{{id}}">View Tracks</a>
    </li>
    {{/each}}
</ul>