<ul>
    {{#each items}}
    <li>{{this.name}}</li>
    <img src="{{this.images.0.url}}" alt="{{this.name}}">
    <button><a href="/albums/{{this.id}}">View Tracks</a></button>

    {{/each}}

</ul> 

