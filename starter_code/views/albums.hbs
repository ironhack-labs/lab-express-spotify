<h1>Albums for: {{ albums.[0].artists.[0].name }}</h1>

{{#each albums as |album|}}
  <img src="{{album.images.[0].url}}" alt="{{album.name}}" width="200">
  <h2>{{album.name}}</h2>
  <p><a href="/tracks/{{album.id}}">Tracks</a></p>
  <hr>
{{/each}}