<div class="artists-list">
    {{#each this.body.items}}
        <div class="artist">
            <img src="{{images.0.url}}"/>   
            <h2>
                {{name}}
            </h2>
            <a class="btn" href="/albums/{{id}}">View Tracks </a>
        </div>
    {{/each}}
</div>