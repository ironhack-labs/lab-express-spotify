{{#each arrayTracks}}
  <div class="container row d-flex justify-content-around"></div>
    <h1 class="artist-name">{{this.name}}</h1>
    <audio controls src="{{this.preview_url}}"><code>audio</code>element</audio>
  </div>
{{/each}}