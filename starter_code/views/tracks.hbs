<section class="tracks">
    <div class="titleTracks">
        <span>TITLE</span>
        <span>LISTEN</span>
    </div>
      {{#each canciones}}
      <div class="track">
          <h1>{{this.name}}</h1>
          <audio id="" controls src="{{this.preview_url}}"></audio>
      </div>
      {{/each}}
</section>