<div class="artist-container">
    {{#each data}}
    <div id="box">
        <h2>{{this.name}}</h2>
        <audio controls>
            <source src="{{this.preview_url}}">
        </audio>
        {{!-- {{#each this.images}} --}}
        {{!-- {{/each}} --}}
    </div>
    {{/each}}

    {{!-- {{data.[0].name}}
    <img src={{data.[0].images.[0].url}}> --}}
</div>