<div class="trackslist">
  {{#each tracks}}
  <p>{{this.name}}</p>
  <figure>
    <audio
        controls
        src={{this.preview_url}}>
            Your browser does not support the
            <code>audio</code> element.
    </audio>
</figure>
  
  {{/each}}
</div>