<div>
    {{#each tracks}}
    <h2>{{name}}</h2>
    <audio
        controls src={{this.preview_url}}>
            Your browser does not support the
            <code>audio</code> element.
    </audio>
    {{/each}}
</div>
