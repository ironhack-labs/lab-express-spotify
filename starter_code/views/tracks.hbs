<div class="cards-container-tracks">
    {{#each trackInfo}}
    {{!--     <div class="card" style="width: 33.333%;">
        <img class="card-img-top" src="{{this.photo}}" alt="Card image cap">
        <div class="card-body">
            <h5 class="card-title">{{this.name}} {{this.lastName}}</h5>
            <p class="card-text">{{this.team}}</p>
        </div>
    </div> --}}

    {{>trackCard}}
    {{!-- partialCall - for that we need to alocate the partials within a folder and reference the url in app.js --}}
    {{/each}}
</div>