<div>
   
    <h2>Results</h2>
   

    <ul>
        {{#each artists}}
        <li>{{this.name}}</li>
        <button>view albums</button>
        <img src="{{this.images.0.url}}">
        {{/each}}
    </ul>

</div>