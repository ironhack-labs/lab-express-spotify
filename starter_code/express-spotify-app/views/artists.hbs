<div class="container">
  <div class="col-12">
    <ul>
      {{#each items as |item|}}
      <li>{{item.name}} <img src="{{ item.images.2.url }}"></li>
      <a href="/artists/{{item.id}}">View albums</a>
      {{/each}}
    </ul>
  </div>
</div>