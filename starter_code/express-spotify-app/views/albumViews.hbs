<div class="artist">
    {{#each this}}
    <div>
    <img src="{{this.images.0.url}}" alt="">
    <div class"artist-box">
        <p>{{this.name}}</p>
        <a href="/track/{{this.id}}">View More</a>
    {{/each}}

</div>