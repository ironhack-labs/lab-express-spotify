

<div class="artist">
 {{#each artists}}
    <h1 class="artist-name">{{this.artist}}</h1>
   <img class="artist-img" src='{{this.images.1.url}}' alt="artist image">
    <h2 class="name">{{this.name}}</h2>
 {{/each}}
</div>

