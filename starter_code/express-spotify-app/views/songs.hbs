<div>

    {{#each songs}}
    <h1>{{this.name}}</h1>
    <figure>
        <figcaption>{{this.name}}</figcaption>
        <audio 
            controls
            src="{{this.preview_url}}">

        </audio>
    </figure>

    {{/each}}

</div>