<div class="track-container">
    <label for="t-rex-roar">{{this.name}}</label>
    <audio id="t-rex-roar" controls src="{{this.preview_url}}">
        Your browser does not support the <code>audio</code> element.
    </audio>
</div>