 <div>
        <img src="{{this.images.[0].url}}" alt="">
        <div>
            <h5>{{this.name}}</h5>
            <form action="/tracks">
                <button type ="button"><a href="/tracks/{{this.id}}">View Tracks</a></button> 
            </form>
            
        </div>
</div>