<ul>
{{#each items}}
    <li>

        {{name}}  and his id is {{id}}
        <br>
        <img style="width:300px" src="{{images.0.url}}" /> 
        <a href="/albums/{{id}}">View Albums</a>
    </li> 
{{/each}}
</ul>