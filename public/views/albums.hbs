
<h1>Albums for {{artistName}}</h1>


{{#if albums}}
  <ul>
    
    {{#each albums}}
      <li>
        <h2>{{name}}</h2>
        
        {{#if images}}
          <img src="{{images.[0].url}}" alt="{{name}} Cover" width="200">
        {{/if}}
        <a href="/tracks/{{id}}">View Tracks</a>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>No albums found for this artist.</p>
{{/if}}
<a href="/tracks/{{id}}">View Tracks</a>