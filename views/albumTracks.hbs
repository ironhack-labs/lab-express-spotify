<h1>Tracks in {{this.album}}:</h1>

{{#each this.tracks}}
<p>{{this.name}}</p>
<audio controls>
    <source src="{{this.preview_url}}" type="audio/mpeg">
    Your browser does not support the audio element.
</audio>
{{/each}}