<h1>Found artitsts</h1>
{{#each searchResults}}
  {{id}}
  {{name}}
  <img src={{images.0.url}} />
  <a href="/albums/{{id}}">Get Albums</a>
  
  <form action="/albums/{{id}}" method="GET">
    <button type="submit">Get Albums</button>
  </form>
{{/each}}