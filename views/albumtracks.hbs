<nav>
  <ul>
    <li id="track-title">Click to hear track samples</li>
  </ul>
</nav>

<div>
  {{#each data}}
    <div id="audio-tracks">
      {{this.name}}
      <audio controls src="{{this.preview_url}}">This track did not load.</audio>
    </div>

  {{/each}}
</div>