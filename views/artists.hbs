<div class="container">
    <h1>Artists</h1>
    <div class="row">
       {{#each artists}}
       <div class="col-4">
        {{>artistResults}}
       </div>       
        {{/each}}
    </div>
</div>