
<div class="list">
    {{#each info}}

    <div class="box">
        <img src="{{this.images.1.url}}" alt="image">
        {{this.name}}
        <a href="/albums/{{this.id}}">View albuns</a>
    </div>

    {{/each}}
</div>



