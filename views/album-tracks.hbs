<div class="track-container">
  {{#each track}}
    <div class="track">
      <p>{{this.name}}</p>
      <audio controls>
        <source src="{{this.preview_url}}" type="audio/mpeg" />
      </audio>
    </div>
  {{/each}}
</div>