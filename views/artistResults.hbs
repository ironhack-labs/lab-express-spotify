<h1>Spotify</h1>
<div>
    {{#each searchedArtist}}

    <h2>
        {{name}}
    </h2>

    <div>
        <img src="{{images.[0].url}}">
    </div>

    <a href="/albums/{{this.id}}">album</a>

    {{/each}}
</div>