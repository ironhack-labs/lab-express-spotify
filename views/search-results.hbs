<div class="container d-flex justify-content-center align-items mt-5 mb-5">
    <h1 class="title">Resultado de búsqueda</h1>
</div>

<div class="container">
    <div class="row">
        {{#each artist}}
        <div class="col-md-4 mb-3">
            <div class="card card-body">
                <img src="{{images.0.url}}" class="card-img-top" alt="{{artista}}">
                <h5 class="card-title">{{name}}</h5>
                <a href="/albums/{{id}}" class="btn btn-primary">View Album</a>
            </div>
            <p>"</p>
        </div>
        {{/each}}
    </div>
</div>