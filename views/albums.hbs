{{#each albums}}

<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{name}}</h5>
    <img class="card-img-top" src={{images.1.url}}>
    <a href="/tracks/{{id}}" class="btn btn-primary">View Albums</a>
  </div>
</div>

{{/each}}