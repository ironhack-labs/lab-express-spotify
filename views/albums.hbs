<h1>Album for: {{albums.0.artists.0.name}} </h1>
{{#each albums}}
  <div>
    <img src="{{this.images.0.url}}" alt="" />
    <p>{{this.name}}</p>
    <a href="/view-tracks/{{this.id}}">View Tracks</a>
  </div>
{{/each}}