<div class="albums">
    {{#each albums}}
    <div class="card">
        <p>{{ name }}</p>
        <img src="{{ images.0.url }}" alt="{{ name }}" width="100px">
        <a href="/tracks/{{ id }}">See Tracks</a>
    </div>
    {{/each}}
</div>