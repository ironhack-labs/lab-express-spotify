<div class="albums">
  {{#each items}}
    <div class="album">
      <h1>{{this.name}}</h1>
      <img src="{{this.images.1.url}}" alt="{{this.name}}" />
      <a href="/tracks/{{this.id}}">View Tracks</a>
    </div>
  {{/each}}
</div>