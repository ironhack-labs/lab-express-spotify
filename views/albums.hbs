
    <div class="div-albums">
        {{#each albums}}
      <h1 class="specific-album-heading">{{this.name}}</h1>
      <img class="specific-album-img" src={{this.images.0.url}} />
      <a class="specific-album-button" href="/tracks/{{this.id}}">View Tracks</a> 
      {{/each}}
    </div>
