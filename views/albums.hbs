<ul>
  {{#each this.albums}}
    <li>  
      <img src="{{images.[0].url}}" width="80">
      {{name}}
      
      <div>
        <a href="/artist/{{artists.[0].id}}/album/{{id}}/tracks/">View Tracks</a>
      </div>
    </li>
  {{/each}}
</ul>