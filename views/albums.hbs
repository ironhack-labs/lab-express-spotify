<div>
    {{#each data}}
        <img src="{{this.images.0.url}}" alt="album_image">
        <h3>{{this.name}}</h3>
        <button> <a href="/tracks/{{this.id}}">View Tracks</a></button>
    {{/each}}
</div>