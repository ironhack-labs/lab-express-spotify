<h2>Albums</h2>

{{#each albumData}}
    <div>
        <h2>{{this.name}}</h2>
        <img src="{{this.images.[0].url}}">
        <button><a href="/tracks/{{this.id}}">View Tracks</a></button>
    </div>
{{/each}}