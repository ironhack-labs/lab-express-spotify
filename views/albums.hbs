<div class='list-page'>
  {{#each albumsList}}
    <div class='list-card'>
      <img src='{{images.1.url}}' alt='{{name}}' />
      <div class='card-info'>
        <p>{{name}}</p>
        <a href='/tracks/{{id}}'>View Tracks</a>
      </div>
    </div>
  {{/each}}
</div>