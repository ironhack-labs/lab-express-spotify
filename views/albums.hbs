<h1>View Albums from {{artistName}}</h1>

<ul>
    {{#each albums}}
    <li>
        <img src="{{images.0.url}}" alt="{{name}} album image" width="300px">
        {{name}}
        <a href='/tracks/{{id}}'>View Tracks</a>
    </li>
    {{!-- images.0.whateverProperty --}}
    {{/each}}
</ul>