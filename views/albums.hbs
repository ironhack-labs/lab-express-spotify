<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Albums</title>
</head>
<body>
    <h1>Albums</h1>
    {{#each albums}}
        <div class="artist">
            <div class="artist-img">
                {{#if images}}
                    <img class="artist-img" src={{images.[1].url}}></img>
                {{/if}}
            </div>
            <h2>{{name}}</h2>
            <a class="albums" href="/tracks/albumId={{id}}">View Tracks</a>
        </div>

    {{/each}}
</body>
</html>