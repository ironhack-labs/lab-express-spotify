<h1>Albums of chosen artist</h1>
{{#each albums}}
<div class="albums">
    <h2>{{name}}</h2>
    <img src="{{images.0.url}}" alt="album cover, url: {{images.0.url}}">
    <a href="/tracks/{{id}}">Go to tracks</a>
</div>
{{/each}}