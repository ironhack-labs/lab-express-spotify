<div>
    <p>Albums for: {{albums.[0].artists.[0].name}}</p>
</div>
{{#each albums}}
    <div>
        <p>{{this.name}}</p>
        <img src="{{this.images.[1].url}}" alt="{{this.name}}">
        <a href="/tracks/{{this.id}}">See the Tracks</a>
    </div>
{{/each}}