<h1>Albums for {{this.name}}</h1>

<div>
    {{#each}}

       <img src="{{images.0.url}}">
        <a href="/albums/{{this.id}}/{{this.album}}/tracks" name="viewTracksBtn" class="viewTracksBtn">View Tracks</a>

    {{/each}}
</div>