    <div>
        {{#each albums}}
            <h1>{{name}}</h1>
            <img src="{{images.1.url}}" alt="{{name}}">
            <a href="/tracks/{{id}}">Check the tracks!</a>
        {{/each}}
    </div>