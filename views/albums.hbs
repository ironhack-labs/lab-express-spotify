<main id="albums">
    {{#each this.items}}
        <div>
            <img src="{{images.[1].url}}" alt="singer-image">
            <div>
                <h4>{{name}}</h4>
                <a href="/albums/{{id}}/tracks">View Tracks</a>
            </div>
        </div>
    {{/each}}
</main>