<ul>
  {{#each artists.items}}
    <li>
      <img class = " artists-img" src="{{this.images.[2].url}}" alt="{{this.name}}">
      <h2>{{this.name}}</h2>
      <a href="/albums/{{this.id}}">View Albums</a>
    </li>
  {{/each}}
</ul>