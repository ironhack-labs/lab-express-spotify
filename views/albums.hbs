

{{#each albumData}}
<div class="result-container">

    <img src="{{this.images.[1].url}}">

    <div class="colored-album">
        <h2>{{this.name}}</h2>

        <button class="rounded-btn" type="submit" onclick="window.location.href='/tracks/{{this.id}}'">See tracks</button>
    </div>

</div>
{{/each}}