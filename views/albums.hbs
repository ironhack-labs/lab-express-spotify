{{!-- {{#each albums}}
<p>{{name}}</p>
<img src="{{images.[0].url}}" alt="Album Cover">
<button><a href="/track-information/{{id}}">View Tracks</a></button>
{{/each}} --}}


 <div class="container">
    <div class="row">
        {{#each albums}}
        <div class="col-md-6 mb-2" >
            <div class="info">
                <img class="imagen" src="{{images.[0].url}}" alt="Album Cover">
                <h2>{{name}}</h2>
                <a href="/track-information/{{id}}" class="btn btn-link">View Tracks</a></button>
            </div>
        </div>
        {{/each}}
    </div>