<div >
    {{#each items}}
    <div>
        {{#each this.images}}
            {{#if @first}}
                <img src="{{this.url}}" alt=""> 
            {{/if}}
        {{/each}}
        <h3>{{this.name}}</h3>
        <a href="/tracks/{{this.id}}">View Albums</a>
    </div>
    {{/each}}
</div>