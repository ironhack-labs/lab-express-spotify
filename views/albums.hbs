<!--{{#each albums}}
  <div>
    <h3>{{name}}</h3>
    <img src="{{images.[0].url}}" alt="{{name}}">
  </div>
{{/each}}

{{#each albums.items}}
  <div>
    <img src="{{images.[0].url}}" alt="{{name}}">
    <h3>{{name}}</h3>
    <a href="/tracks/{{id}}">View Tracks</a>
  </div>
{{/each}}-->
{{#each albums}}
  <div>
    <h3>{{name}}</h3>
    <img src="{{images.[1].url}}" alt="{{name}}">
    <a href="/tracks/{{id}}">View tracks</a>
  </div>
{{/each}}