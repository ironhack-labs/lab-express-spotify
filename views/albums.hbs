{{#each albums}}
    <h1>{{name}}</h1>
    {{#if images.0.url}}
    <p><img src="{{images.0.url}}" /></p>
    {{/if}}
    <p><a href="/tracks/{{id}}" name="tracks" method="get">View the Tracks</a></p>

{{/each}}