<div>
    {{#each albums}}
    <h2>{{this.name}}</h2>
    <img src={{this.images.[0].url}}> {{!-- Dot before index --}}
    <br>
    <a href="/tracks/{{this.id}}">View tracks</a>

    {{/each}} 

</div>