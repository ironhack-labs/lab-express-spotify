<div class="container row wrap">

    {{#each items}}

        <div class="card card-big container column align-center">
            <img src="{{images.[1].url}}" alt="">
            <h4>{{name}}</h4>
            <a href="/albums/tracks/{{id}}">View Tracks</a>
        </div>

    {{/each}}

</div>
