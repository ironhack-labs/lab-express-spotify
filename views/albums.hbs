<h1>Artists</h1>

<div>
    {{#each results}}
    {{this.name}}
     <img src="{{this.images.1.url}}"/>
 <a href="/tracks/{{this.id}}">View Tracks</a> {{!-- this.id le pasa al track la id del album --}}
    {{/each}}
</div>