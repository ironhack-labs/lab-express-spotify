<!-- layout.hbs -->

<div class="container">
  <div class="row">
    {{#each albumArray}}
      <div class="acard col d-flex justify-content-center">
        <div class="acard-image col">
          {{#each images}}
            {{#if @first}}
              <img src="{{this.url}}">
            {{/if}}
          {{/each}}
        </div>
        <div class="acard-info col">
          <h5>{{this.name}}</h5>
          <a href="#"><button class="btn">Tracks</button></a>
        </div>
      </div>
    {{/each}}
  </div>
</div>
