<div class="albums">

  {{#each albums}}
  <div class="artist">
    <img src="{{this.images.[0].url}}" width="50" height="50">
    <div class="text-artist">
      <a href="">{{this.name}}</a>
      <a href="/tracks/{{this.id}}">View tracks</a>
    </div>
  </div>
  {{/each}}
</div> 