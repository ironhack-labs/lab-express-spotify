<div class="container">
    <div class="row">
        {{#each items}}
        <div class="col-4">
            <img src="{{images.[1].url}}" alt="">
            <p class="albums-name">{{name}}</p>
        </div>
        {{/each}}
    </div>
</div>