<div class="albums">
    <h1></h1>
    {{#each result}}
    <h2>{{this.name}}</h2>
    <br>
    <img src={{this.images.[0].url}} alt="Album Cover">
    <a href="/tracks/{{this.id}}">View Tracks</a>
    {{/each}}
</div>