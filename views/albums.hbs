{{#each albums}}
  <div class="album">
    <h2>{{name}}</h2>
    {{#if images.length}}
      <img src="{{images.[0].url}}" alt="{{name}} image">
    {{else}}
      <p>No image available</p>
    {{/if}}
    <a href="/tracks/{{id}}">View Tracks</a>
  </div>
{{/each}}