<div>
  <h1>Albuns</h1>
  {{#each albums}}
  <div>
    <ul>
      {{ this.name}}
      <img src="{{this.images.[0].url}}">
      <a href="/tracks/{{this.id}}">View Tracks</a>
    </ul>
    {{/each}}
  </div>
</div>