<div class="estruct">
{{#each albums}}
 <div class="albums" align="center">
<img alt="{{name}}" class="img" src="{{images.0.url}}" />
<h1>{{name}}</h1>
<a href="/tracks/{{id}}" class="view" >View Tracks </a>
<h4>{{cover}}</h4>
</div>
{{/each}}
</div>