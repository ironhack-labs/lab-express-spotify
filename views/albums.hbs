<h1>Albums:</h1>

<ul>
    {{#each this.items}}
    <li>
        <h3>{{name}}</h3>
        <img src="{{images.0.url}}"></button>
        <a href="/{{id}}">View Tracks</a>
    </li>
    {{/each}}
</ul>