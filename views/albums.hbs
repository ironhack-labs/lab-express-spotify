<div>
  <h1>Albums</h1>
  {{#each albums}}
    <div>
      <img src="{{images.0.url}}" alt="" width="200" />
      <div>{{name}}</div>
      <a href="/tracks/{{this.id}}">View Tracks</a>
    </div>
  {{/each}}
</div>