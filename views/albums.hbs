{{#each artistAlbumsArray}}
	<article>
		<img src='{{this.images.0.url}}' alt='ALBUM COVER' />
		<h3>{{this.name}}</h3>
		<a class='view-tracks-button' href='/tracks/{{this.id}}'>View Tracks</a>
	</article>
{{/each}}