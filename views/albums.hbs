<div class="result-wrapper">
{{#each data.body.items}}
<div class="album">
{{#each this.images}} {{#if @first}}<img src="{{this.url}}"{{/if}}{{/each}}
 alt="{{this.name}}">
    <h1>{{this.name}}</h1>
    {{#each this.artists}}<h2>{{this.name}}</h2>{{/each}}
<a href="/view-tracks/{{this.id}}">View Tracks</a>
</div>
{{/each}}
</div>