{{#each albums}}

  {{name}}
  <img
    src={{images.0.url}}
    height={{images.0.height}}
    width={{images.0.width}}
  />
  <a href='/tracks/{{id}}' type='button' class='btn btn-dark'>
    Show the tracks
  </a>
{{/each}}