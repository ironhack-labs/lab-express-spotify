
{{#each albums}}
    <div>
        <img src="{{this.images.[0].url}}" alt="album cover">
        <p>{{this.name}}</p>
        <button><a href="/tracks/{{this.id}}">View Tracks</a></button>
    </div>
{{/each}}


