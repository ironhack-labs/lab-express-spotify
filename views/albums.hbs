{{#each searchResults}}
  <section class="result-container">
    <div class="result-box">
      <img src="{{images.1.url}}" />
      <h3>{{this.name}}</h3>
      <a href="/album-tracks/{{this.id}}">View this Album</a>
    </div>
  </section>
{{/each}}