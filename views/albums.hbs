{{#each this}}
<div>
    <div>
        <img src="{{images.[0].[url]}}" alt="image artist">
    </div>
    <div>
        <h2>{{name}}</h2>
        <a href="/track-information/{{id}}">View Tracks</a>
    </div>
</div>
{{/each}}