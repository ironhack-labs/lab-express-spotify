<div class="albums-container">
  {{#each albums}}
    <div class="album">
      <img src="{{images.1.url}}" alt="" />
      <h2>{{name}}</h2>
      <a href="/tracks/{{id}}">View tracks</a>
    </div>
  {{/each}}
</div>