<div class="albums">
    {{#each result}}
    <h1>{{this.name}}</h1>
    <br>
    <img src={{this.images.[0].url}} alt="Albums">
    <a href="/tracks/{{this.id}}">View Tracks</a>
    {{/each}}
</div> 