<h1>Albumsfor: {{dataArtists.name}}</h1>
<div>
    {{#each artistAlbums}}
    <div>
        <img src="{{images.[0].url}}">
        <div >
            <h1>{{name}}</h1>
            <a href="/tracks/{{id}}">View Tracks</a>
        </div>
    </div>
{{/each}}
</div>