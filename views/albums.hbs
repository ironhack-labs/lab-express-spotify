{{!-- <h2>Albums for: {{this.artistName}}</h2>
<div class="row">
{{#each albumsList}}
  <div class="col-md-4">
    <div class="card">
      <img class="card-img-top" src="{{this.images.[0].url}}" alt="{{this.name}}" />
      <div class="card-body">
        <h5 class="card-title">
          {{this.name}}
        </h5>
        <a href="/tracks/{{this.id}}" class="btn btn-primary">View Tracks</a>
      </div>

    </div>
  </div>
{{/each}}
</div> --}}