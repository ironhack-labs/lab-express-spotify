{{#each albums}}
  <h2>{{name}}</h2>
  {{#if this.images}}
    <img
      src="{{images.0.url}}"
      alt="{{name}}"
      style="width:100px; heigth:100px"
    />
  {{/if}}
  <a href="/tracks/{{this.id}}">View Tracks</a>
{{/each}}