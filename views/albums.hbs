<div id='artistWrapperDiv'>
	{{#each data.body.items}}
		<div class='artist-div'>

			<p>{{name}}</p>
			<img src='{{images.0.url}}' alt='{{name}}' />
			<a href='/tracks/{{id}}'>View Tracks</a>
			<p>{{id}}</p>
		</div>
	{{/each}}
</div>
