{{#each albums as |album|}}
  <div class="albums">
    <img src="{{images.0.url}}" alt="{{album.name}}" />
    <h3>{{album.name}}</h3>
    <button><a class="click" href="/tracks/{{album.id}}">View Tracks</a></button>
  </div>
{{/each}}