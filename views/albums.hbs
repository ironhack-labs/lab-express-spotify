<div id="albums">
    {{#each items}}
    <div id="box-albumsInfo">
    <img src="{{images.0.url}}" alt="image1" id="albumImg">
    <h1 id="albumName">{{name}}</h1>
    <a href="/tracks/{{id}}"><input type="button" value="View Tracks" id="viewTracks"></a>
    </div>
    {{/each}}
</div>

