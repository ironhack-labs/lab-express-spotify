<div class="columns-3"
<ul>
    {{#each albums}}
    <li>
        {{this.name}}
        <br>
        <img src="{{this.images.0.url}}" alt="{{this.name}}">
        <br />
        <a href="/tracks/{{this.id}}"></a>
    </li>
    {{/each}}
</ul>