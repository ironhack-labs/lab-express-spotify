
<h1 id="albums">Albums {{this.name}} </h1>

<ul id="ul-albums">
  {{#each albums}}
    <li>
      <img src="{{this.images.[0].url}}" alt="Album Cover">
      <h3>{{this.name}}</h3>
      <a href="/tracks/{{this.id}}">View Tracks</a>
    </li>
  {{/each}}
</ul>