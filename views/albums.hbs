<div id="albums-container">
    {{#each albums}}
        <div id="album">
            <img src="{{images.0.url}}">
            <h2>{{name}}</h2>
            <a href="/tracks/{{id}}">View Tracks</a>
        </div>
    {{/each}}
</div>