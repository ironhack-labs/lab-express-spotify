<div class="container">
    <h1>ALBUMS:</h1>
    <hr>

    {{#each album}}
    <h2>{{name}}</h2>

    {{!-- para acceder a una posición del array, utilizar dot notation --}}
    <img src="{{images.0.url}}" alt="imagen">

    {{!-- en este caso no es _id, porque en la API de Spoty se llama id --}}
    {{!-- cuando es una BBDD de mongoDB, podemos llamarlo id o _id, es lo mismo --}}

    <a href="/{{id}}/tracks/">View tracks</a>

    {{/each}}
</div>