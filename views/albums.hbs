{{#each albums }}
<div id="album-container">
  <img src="{{this.images.1.url}}" alt="">
  <div class="info-text-container">
    <p>{{this.name}}</p>
    <a href="/album/{{this.id}}" id="view-album-btn">View Tracks</a>
  </div>
</div>

{{/each}}




