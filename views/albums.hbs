{{#each albums}}
<div class="albums">
  <img src="{{this.images.[1].url}}" alt="An image on {{name}}'s album">
  <h1>{{this.name}}</h1>
  <a href="/tracks/{{this.id}}"> View Tracks</a>
</div>
{{/each}}