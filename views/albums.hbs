<h2>Albums</h2>
<ul>
    {{#each albums}} 
        <h2>{{this.name}}</h2>
        <img src="{{this.images.0.url}}" alt="image">
        <a href="/tracks/{{this.id}}">View Tracks</a>
        {{/each}}

   
</ul>