<div class="container">

  <div class="row">

    <div class="col-sm">
        <div class="grid-title">
          {{#each info}}
          <!-- ask for partials -->
          {{> artistCard this}}
          {{/each}}
        </div>

    </div>

</div>
