<div class="albums">
{{#each albumsInfo}}
  <div >
    <img src="{{this.images.0.url}}" alt="Image" />
    <p>{{this.name}}</p>
    <a href="/tracks/{{this.id}}">View Tracks</a>
  </div>
{{/each}}
</div>