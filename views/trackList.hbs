{{#each tracks}}
    <div class="oneTrack">
        <div>
            <h4>{{name}}</h4>
        </div>
        <div>
            <audio controls > 
            <source src={{preview_url}}>
        </audio>
        </div>
    </div>

{{/each}}