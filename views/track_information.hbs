<div class="">

  {{#each tracks}}
    <img class="" src="{{images.0.url}}" alt="" />
    <h2>{{name}}</h2>
    <audio controls src="{{this.preview_url}}">
      Your browser does not support the
      <code>audio</code>
      element.
    </audio>

  {{/each}}

</div>