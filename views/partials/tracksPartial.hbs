<div class="card">
  <img src="{{this.images.1.url}}" class="card-img-top" alt="artist pic" />
  <div class="card-body">
    <h5 class="card-title">{{this.name}}</h5>
    <a href="/track/{{this.id}}" class="btn btn-primary">View tracks</a>
  </div>
</div>