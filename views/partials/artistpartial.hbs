<article class="col-12 col-md-4 mt-2">
  <div class="border border-0 rounded rounded-3">
    <a href="#">
      <img
        src="{{this.images.0.url}}"
        class="img-thumbnail col-6"
        alt="Poster for "
      />
    </a>
    <h2>{{this.name}}</h2>
    <a href="/albums/{{this.id}}">View Albums</a>
  </div>
</article>