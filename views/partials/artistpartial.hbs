 <div class="artist-partial">
  <div class="artist-details">
    <h3>{{this.name}}</h3>
    <img src="{{this.images.0.url}}" alt="images"> 
    <a href="/albums/:artistId">View Album</a> 
   </div>
</div> 