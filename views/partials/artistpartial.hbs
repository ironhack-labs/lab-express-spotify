<div>
    <div>
        {{#with this.images.[0]}}
            <img src={{url}}/>
        {{/with}}
    </div>
    <div>{{this.name}}</div>
    <button><a href="/albums/{{this.id}}">View Albums</a></button>
    
</div>