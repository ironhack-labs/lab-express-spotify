<div>
  {{#if preview_url}}
  <td>{{ name }}</td>
  <td class="right-column"> <audio controls src="{{ preview_url }}"> </audio></td>

  {{else}}
 <td>{{ name }}</td>
 <td class="right-column"><h3>Audio with no preview avaiable</h3></td>
  {{/if}}
</div>