<div class="search-result-card">
    <div class="img-container">
        <img src="{{this.images.2.url}}" alt="">
        <h1 class="artist-name">{{this.name}}</h1>
    </div>
    <a href="/albums/{{this.id}}" class="view-albums">View Albums</a>
</div>
