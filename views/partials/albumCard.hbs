<div class="col-md-4">
    <div class="card">
        <img src="{{this.images.0.url}}" class="card-img-top" />
        <div class="card-body">
            <h5 class="card-title">{{this.name}}</h5>
            <a href=" /albums/{{this.id}}/tracks" class="btn btn-primary">View Tracks</a>
        </div>
    </div>
</div>