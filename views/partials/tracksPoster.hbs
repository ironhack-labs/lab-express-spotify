<div class="musica">

    <div class="nombre">
          <h3>{{this.name}}</h3>
    </div>

    <div class="play">
  
     <audio
        controls
        src="{{this.preview_url}}" >
            Your browser does not support the
            <code>audio</code> element.
    </audio>

    </div>








</div>