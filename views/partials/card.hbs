<div>
  {{#if images.0.url}}
    <img src="{{images.0.url}}" alt="card-image" />
  {{else}}
    <img src="/images/no-cover.jpg" alt="card-image" />
  {{/if}}

  <h2>{{name}}</h2>
  <a href="/albums/{{id}}">View Albums</a>

</div>