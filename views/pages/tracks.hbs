<div class="tracks-container">
  {{#each tracks as |track|}}

    <div class="track">
      <h1>{{track.name}}</h1>
      <audio controls src="{{track.preview_url}}"></audio>
    </div>

  {{/each}}
</div>