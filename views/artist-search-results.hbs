<h1>Lista artistas: {{busqueda}}</h1>
{{#each items}}
<div>
    {{#each images}}
        {{#if @last}}
            <img src="{{url}}" alt="">
        {{/if}}
    {{/each}}
    <p>{{name}}</p>
    <a href="/albums/{{id}}">View Albums</a>
 </div>
{{/each}}