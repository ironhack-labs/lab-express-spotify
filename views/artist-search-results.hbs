<h1>Search results</h1>
<h1>{{name}}</h1>

{{#each artists}}
    <h3>{{this.name}}</h3>
    {{#if images.length}}
    <img src="{{this.images.[0].url}}">
    {{/if}}
    <br>
    <a href='/albums/{{id}}'>View Albums</a>
{{/each}}
