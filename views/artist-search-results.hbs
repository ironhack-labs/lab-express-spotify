{{#each artistsNames}}
  <div class="card">
    <h2>{{this.name}}</h2>
    <img src="{{this.images.0.url}}" alt="{{this.name}}" />
     <a href="/albums/{{this.id}}">Show Albums</a> 
  </div>
{{/each}}