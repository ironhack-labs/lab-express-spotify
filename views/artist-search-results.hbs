<div class="list">
  {{#each items}}
    <div>
      <p><img src="{{this.images.1.url}}" alt="The Beatles" /></p>
      <p>{{this.name}}</p>
      <a href="/albums/{{this.id}}">View Albums</a>
    </div>
  {{/each}}
</div>