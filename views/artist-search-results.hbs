 {{#each artist}}
        <div class="artistSearchImg">
            <img src="{{images.[0].url}}">
            <p>{{name}}</p>
            <a href="/albums/{{id}}">View albums</a>
        </div>
    {{/each}}
