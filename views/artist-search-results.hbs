{{!-- {{log "received" this}} --}}

<div id="artist-list">

    {{#each this.artists.items}}
        {{!-- {{log this}} --}}
        <div class="artist">
            {{log "artist" this}}
            {{#if this.images}}
                <img src="{{this.images.[0].url}}" alt="{{this.name}}">
            {{/if}}
            {{this.name}}
            <a href="/albums/{{this.id}}">View Albums</a>

        </div>
        {{!-- {{this.name}} --}}
    {{/each}}

</div>