<div class="container">
    <div class="row">
        {{#each response}}
        <h1>{{name}}</h1>
        <img src="{{images.0.url}}" alt="">
        <a href="/albums/{{id}}">View Albums</a>
        {{/each}}
    </div>

</div>