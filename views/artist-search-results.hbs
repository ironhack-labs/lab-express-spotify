<h1>Resultados de búsqueda</h1>

<div class="row">
    {{#each result}}
    <div class="col-md-4">
        <div class="card" style="width: 18rem;">
            <img src="{{this.images.0.url}}" class="card-img-top" alt="...">
            <div class="card-body">
                <h5 class="card-title">{{this.name}}</h5>
                <a href="/album/{{id}}" class="btn btn-primary">View Albums</a>
            </div>
        </div>
    </div>
    {{/each}}
</div>