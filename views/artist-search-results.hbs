{{!-- {{#each artistArray}}
{{>Artistcard this}}
{{/each }} --}}

<div class="container text-center">
    <div class="row">
        {{#each artistArray}}
        {{>Artistcard this}}
        {{/each }}
    </div>
</div>