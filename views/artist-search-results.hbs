<div class="albums-container">
    {{#each artists}}
    <div class="album-container">
        <img src="{{this.images.[0].url}}" alt="">
        <p>{{this.name}}</p>
        <a href="/albums/{{this.id}}">Ver Albums</a>
    </div>
    {{/each}}
</div>