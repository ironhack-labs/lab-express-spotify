<h1>Artist Search Results</h1>
<div>
{{#each this}}

    <img src="{{this.images.[0].url}}" alt="{{this.name}}">
    <h2>{{this.name}}</h2>
    <a href="/albums/{{this.id}}">Albums</a>
    
{{/each}}

 </div>