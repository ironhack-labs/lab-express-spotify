<ul>
    {{#each displayInfo}}
        <li>
        <article id="artists">
            <img src="{{this.images.url}}" alt="{{this.name}}'s Album">
            <p>{{this.name}}</p>
            <a href="/albums/{{this.id}}">View Albums</a>
        </article>
        </li>
   {{/each}}
</ul> 