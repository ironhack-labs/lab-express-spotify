<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <div class="wrapper d-flex flex-row" style="flex-wrap: wrap;">
                    {{#each .}}
                    {{#if name}}
                    {{> card}}
                    {{/if}}
                    {{/each}}
                </div>
            </div>
        </div>
    </div>
</section>