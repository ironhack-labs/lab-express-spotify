{{!-- {{log data}} --}}


{{#each artists}}
    <section class="card">
        <h2>Name: {{this.name}}</h2>
        <img src="{{this.images.0.url}}" alt="" srcset="">
        <a href="/albums/{{id}}">View Albums</a>
        <h1>{{id}}</h1>
    </section>
{{/each}}