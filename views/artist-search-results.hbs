<h1>List of Reference :</h1>

{{#each dataArr}}
    <section class="Ref-summary">
        <img src="{{this.images.[0].url}}">
        <h2>{{this.name}}</h2>
        <a href="/albums/{{this.id}}" ><button>Views Album</button></a>
    </section>
{{/each}}