{{!-- <h1> estas abriendo el artist-search.hbs</h1>

{{#each groups}}
<h1>{{name}}</h1>
{{/each}} --}}

<div class="container">
  <div class="row">
    {{#each groups}}
    <div class="col-md-4 mb-3 ">
      <div class="card card-body">
        <img src="{{images.[0].url}}" alt="">
        <h3>{{name}}</h3>
        <a class="btn btn-primary" href="/albums" role="button">View Albums</a>
      </div>
    </div>
    {{/each}}
  </div>
</div>