<div class="tracks">
 {{#each findTracks}}
  <article>
      <br>
<h2>{{this.name}}</h2>
<audio controls src="{{this.preview_url}}">
    Your browser does not support the
    <code>audio</code>
    element.
  </audio>
  </article>
{{/each}}

</div>