<h1>Tracks</h1>

{{#each tracks}}
  <h3>{{this.name}}</h3>
  <figure>
    <figcaption>Listen to the T-Rex:</figcaption>
    <audio controls src="/preview_url.mp3">
      Your browser does not support the
      <code>audio</code>
      element.
    </audio>
  </figure>
{{/each}}