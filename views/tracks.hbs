
<div>
{{#each results.items}}
    <tr class="table-light">
      <th scope="row">{{track_number}}</th>
      <td>{{name}}</td>
      <td>
        <audio controls src="{{preview_url}}">
           
        </audio>
    </td>
    </tr>
    {{/each}}
    </div>