<ul>
    <h1>Tracks</h1>
    {{#each tracks}}
    <li>
        {{#if this.preview_url}}
        <audio controls>
            <source src="{{this.preview_url}}" type="audio/mpeg"> 
        </audio>
        {{else}}
        <p>No Preview available</p>
        {{/if}}
        </li>
    {{/each}}
</ul>