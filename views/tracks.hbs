<h2>Tracks</h2>

{{#each tracksData}}
    <div>
        <div>
            <p>{{this.name}}</p>
        </div>

        <article>
            <audio controls src="{{this.preview_url}}">
                <a href="{{this.preview_url}}"></a>
            </audio>
        </article>
    </div>
{{/each}}