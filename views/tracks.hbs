<body class="trackBody">
    <div class="tracklist">
    {{#each tracks}}
    <p>{{this.name}}</p>
    <audio controls>
        <source src={{this.preview_url}} />
    </audio>
    {{/each}}
</div>
</body>