<div>
    <div>
        <h1>Title</h1>
        <h1>Listen</h1>
    </div>
    {{#each this}}
    <div>
        <h1>{{this.name}}</h1>
        <audio controls src={{this.preview_url}}></audio>
    </div>
    {{/each}}
</div>