<h1>tracks go here!</h1>

{{#each tracks}}
  {{!-- <h1>{{this.name}}</h1> --}}
  <p>{{this.name}}</p>
  <audio controls src={{this.preview_url}}>
    <a href={{this.preview_url}}>
      Download audio
    </a>
  </audio>
  {{!-- <p>{{this.preview_url}}</p> --}}
{{/each}}