<div>
        <h2>Tracks</h2>
    </div>
 {{#each tracksInfo }}
    <div class="tracks-item">
        <div class="track">
            <p>{{this.name}}</p>
        </div>
    <figure>
        <audio controls src="{{this.preview_url}}">
            <a href="{{this.preview_url}}"></a>
        </audio>
    </figure>
    </div>
{{/each}}