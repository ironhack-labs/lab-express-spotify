<main
  class='container text-center alto d-flex flex-column justify-content-center mt-2'
>
    <div class='row'>
        {{#each tracks}}
            {{> trackspartial}}
        {{/each}}
    </div>
</main>

    