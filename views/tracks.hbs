<div>
  <h2>Track</h2>
  <h2>Preview</h2>
  {{#each tracksInfo}}
    <div>
      <p>{{this.name}}</p>
      <audio controls src="{{this.preview_url}}">
        <a href="{{this.preview_url}}"></a>
      </audio>
    </div>
  {{/each}}
</div>