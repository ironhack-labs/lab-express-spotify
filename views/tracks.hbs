<h1 class="center">All song from {{this.[0].artists.[0].name}}</h1>
{{#each tracks}}
<div class="songcontainer">
    <p>{{this.name}}</p>
    <audio controls src={{this.preview_url}}></audio>
</div>
{{/each}}