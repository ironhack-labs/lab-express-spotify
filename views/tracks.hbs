<div>
  {{#each tracks}}
    <div>
      <p>{{this.name}}</p>
      <audio controls><source src="{{this.preview_url}}" type="audio/mpeg">Not supported in your browser</audio>
    </div>
  {{/each}}
</div>