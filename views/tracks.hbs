{{#each tracks}}

    <figure>
    <figcaption>{{this.name}}</figcaption>
    <audio
        controls
        src="{{this.preview_url}}">
            <a href="{{this.href}}">
                Download audio
            </a>
    </audio>
</figure>
{{/each}}