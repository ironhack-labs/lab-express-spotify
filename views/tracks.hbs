<div class="tracks">
  {{#each items}}
    <div class="track">
      <p>{{this.track_number}} {{this.name}}</p>
      <audio controls>
        <source src="{{this.preview_url}}" type="audio/mpeg" />
      </audio>
    </div>
  {{/each}}
</div>