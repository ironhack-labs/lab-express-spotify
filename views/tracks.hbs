<h3>this is the tracks page</h3>


{{#each items}}
    <section class="tracks-summary">
        <h2>{{this.name}}</h2>
       <br>

    <figure>
    <figcaption>Listen to the audio:</figcaption>
    <audio
        controls
        src="{{this.preview_url}}">
            <a href="{{this.preview_url}}">
                Download audio
            </a>
    </audio>
</figure>

    
    
        </section>
    {{/each}}
