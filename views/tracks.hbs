{{#each tracks}}
  <div class='track-item'>
    <h4>{{name}}</h4>

    {{#if preview_url}}
      <audio controls>
        <source src={{preview_url}} />
      </audio>
    {{else}}
      <h5>Track not available</h5>
    {{/if}}
  </div>
{{/each}}