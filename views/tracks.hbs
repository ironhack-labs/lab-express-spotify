<p>Here are the tracks!</p>
<ul>
    {{#each items}}
    <li><p>{{this.name}}</p> <br>
    <audio
        controls
        src="{{this.preview_url}}">
            <a href="{{this.preview_url}}">
                Download audio
            </a>
    </audio></li>
    {{/each}}
</ul>