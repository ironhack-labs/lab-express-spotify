<div id="tracks-results">
    {{#each tracks}}
    <div id="track-card">
        <p>{{this.name}}</p>
        <audio
            controls
            src="{{this.preview_url}}">
            {{!-- <a href="{{this.preview_url}}">
            Play audio
            </a> --}}
        </audio>
        <br>
    </div>
    {{/each}}
</div>