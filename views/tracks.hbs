{{#each info}}

<div>{{this.name}}</div>

<img src="{{this.images.1.url}}">

<a href="/tracks/{{this.id}}">
    <button type="submit" class="btn btn-outline-primary">Tracks</button>
</a>

<audio controls>
    <source src="{{this.preview_url}}">
</audio>

{{/each}}