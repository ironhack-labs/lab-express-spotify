{{#each tracks}}
    {{!-- {{this.name}}
    <audio controls>
        <src="{{this.preview_url}}" type="audio">
        Preview your favorite song
    </audio> --}}
    <figure>
    <figcaption>{{this.name}}</figcaption>
    <audio
        controls
        src="{{this.preview_url}}" type="audio/mpeg">
            Your browser does not support the
            <code>audio</code> element.
    </audio>
    </figure>
{{/each}}
