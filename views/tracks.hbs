<div>
{{#each tracks}}
<div class="track">
    <h4>{{name}}</h4>
    <audio
        controls
        src="{{preview_url}}">
            Your browser does not support the
            <code>audio</code> element.
    </audio>

</div>
{{/each}}
</div> 