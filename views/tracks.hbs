<h1>Preview the tracks</h1>
{{#each tracks}}
<figure>
    <figcaption>Listen to {{name}}</figcaption>
    <audio controls src="{{preview_url}}">
        <a href="{{preview_url}}"> Download audio </a>
    </audio>
</figure>

<hr>
{{/each}}