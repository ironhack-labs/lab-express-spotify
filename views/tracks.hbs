<div class="header">
  <span>Title</span>
  <span>Listen</span>
</div>

<div class="tracks-container">
  {{#each tracks}}

    <div class="track">
      <span>
        {{name}}
      </span>
      <audio controls>
        <source src="{{preview_url}}" />
      </audio>
    </div>
  {{/each}}
</div>