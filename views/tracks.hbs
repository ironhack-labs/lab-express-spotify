<h1>Tracks</h1>


{{#each tracksData.body.items}}
    <section class="card">
        <h2>Name: {{name}}</h2>
    </section>
    <audio
        controls
        src="{{preview_url}}">
    </audio>
{{/each}}