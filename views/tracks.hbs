<div>
    <div class="title">
        <h1>Title</h1>
        <h1>Listen</h1>
    </div>
    <div class="tracks">
        {{#each tracks}}
        <div class="controls">
            <h1>{{name}}</h1>
            <audio controls src="{{preview_url}}">
                Your browser does not support the
                <code>audio</code> element.
            </audio>
        </div>
        {{/each}}
    </div>
</div>