<h2>Tracks of {{tracks.artist}}</h2>

<div class="results">

    {{#each tracks.trackArr}}
    <div>         
        <h3>{{name}}</h3>
        <audio controls="controls" src="{{preview_url}}" type="audio/mpeg"></audio>
    </div>
    {{/each}}
</div>