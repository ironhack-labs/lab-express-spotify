<h1>Tracks:</h1>

<ul>
    {{#each this.items}}
    <li>
        <h3>{{name}}</h3>
        <audio controls src="{{preview_url}}">
            <a href="{{preview_url}}">
                Download audio
            </a>
        </audio>
    </li>
    {{/each}}
</ul>