<div class="search results">
    <h1>View Tracks</h1>

    <ul class="tracks">
        {{#each tracks}}
        <li>
            {{name}} <audio controls src={{preview_url}}>
                Your browser does not support the
                <code>audio</code> element.</audio>
        </li>
        {{/each}}
    </ul>
</div>