<div class="container">
    <div class="row">
        {{#each tracks}}
        <div class="album">
          <p> {{name}} </p>
            <audio controls src="{{preview_url}}">Audiovorschau</audio>
        </div>
        {{/each}}
    </div>
</div> 