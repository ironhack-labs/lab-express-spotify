<div>
    {{#each tracks}}
        <div>
            <h1>{{this.name}}</h1>
            <audio controls src="{{this.preview_url}}"> Your browser does not support the <code>audio</code> element. </audio>
        </div>
    {{/each}}
</div>