<div class="tracks-container">
    {{#each tracks}}
        <div class="track">
            <p>{{this.name}}</p>
            <audio controls><source src="{{this.preview_url}}"></audio>
        </div>
    {{/each}}
</div>