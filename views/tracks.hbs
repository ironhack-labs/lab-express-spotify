<ul class="tracks">
    <div>Title</div>
{{#each items}}
<li>
    {{this.name}}
        <audio controls src="{{this.preview_url}}.mp3">
            Your browser does not support the
            <code>audio</code> element.
    </audio>
</li>
{{/each}}
</ul>