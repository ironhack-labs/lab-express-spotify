{{#each data}}
    <div class = "cover-view">
        <img src="{{this.images.[1].url}}" alt="artist image">
        <p>{{this.name}}</p>
        <a href="/albums/{{this.id}}">View Albums</a>
    </div>
{{/each}}